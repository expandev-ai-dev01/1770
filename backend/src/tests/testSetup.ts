// This file is intended for global test setup.
// For example, you can configure testing libraries like Jest or Vitest here.

// Example for Jest:
/*
beforeAll(() => {
  // Runs once before all tests
  console.log('Setting up global test environment...');
});

afterAll(() => {
  // Runs once after all tests
  console.log('Tearing down global test environment...');
});
*/
